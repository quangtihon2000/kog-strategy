# Required
TELEGRAM_BOT_TOKEN=123456789:AA-replace-with-BotFather-token
TELEGRAM_ALLOWED_USER_IDS=123456789,987654321

# Optional — where monitor alerts fan out. User ids (DM) or group/channel
# ids (negative, e.g. -1001234567890). Empty = falls back to ALLOWED_USER_IDS.
# TELEGRAM_ALERT_CHAT_IDS=-1001234567890

# Optional — base URL for the per-signal stats page. New-signal notifications
# append `?ts=<timestamp>` and include a clickable link. Defaults to the URL
# below; set explicitly to override, or to empty string to disable.
# TELEGRAM_SIGNAL_STATS_URL=https://quangtihon2000.github.io/conde-stats/

# Optional (defaults shown)
# REDIS_URL=redis://localhost:6379
# LOG_LEVEL=INFO
# FLEET_CONFIG=./fleet.yaml
